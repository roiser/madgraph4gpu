name: ghci

on:
  push:
    branches: ghci

jobs:
  test:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v4
    - name: hello world
      run: echo hello world && echo `pwd` && echo `ls`
